# 2026-04-14 财务页采用轻卡片收口

## 状态

- `confirmed`

## 背景

- 财务页当前仍残留渐变背景、粗边框和强调过强的块状区域。
- 这与配送员端既有 UI Manifest 中“弱边界、少装饰、突出数字与主操作”的方向不一致。

## 最终结论

1. 财务页的摘要区、确认区优先采用浅底白卡 + 弱边框。
2. 状态强调通过文字、轻底色和局部品牌色完成，不使用大面积渐变。
3. 财务视觉优化按小步进行，不在单次改动里连带重构数据结构或交互流程。

## 影响

- 本轮先影响 `finance-daily-close.html` 的对账确认区。
- 后续再逐步收口支付方式区和其他残留重视觉区域。
