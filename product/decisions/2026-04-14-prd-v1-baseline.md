# 2026-04-14 PRD V1 基线决策

## 状态

- `confirmed`

## 决策

1. 将 `product/prd/2026-04-14-lpg-vendor-prd-v1.md` 作为后续开发与重构的上位基线。
2. 后续任务若涉及跨模块取舍（订单/库存/安检/财务），以该 PRD 的闭环和版本边界优先。
3. 新需求若与该 PRD 冲突，先更新 `product/decisions/` 再进入实现。
4. V1 语音开单采用字段级强解析，不使用弱解析过渡方案。
5. V1 平台端仅交付配置与安检/气瓶合规监控，不进入运营报表。
6. 气瓶扫码策略由平台配置下发（区域级可选/强制）。
7. 平台端不关注完单、库存、财务等经营数据，只关注安检覆盖、隐患与气瓶状态。
8. 移动端同步不做重试监控，只保留低配离线列表入口供配送员手动提交。
9. 平台端仅管理已配置扫码区域，不做全量安检策略监控。

## 影响

- 影响范围：`product/` 与 `.trellis/tasks/` 的后续输入引用方式
- 不直接改动：现有功能代码与接口契约
