# Interaction

本目录记录页面职责、导航归属、交互层次与状态反馈规则，用于防止“功能都做了，但页面职责变得混乱”。

## 关注点

- 入口归属：功能应放在 `delivery-app` 还是 `platform`
- 页面职责：该页只负责查看、执行、配置还是监控
- 信息优先级：什么必须首屏可见，什么应该次级展开
- 状态反馈：加载、失败、离线、可撤销、同步中如何表达

## 适用原则

- `delivery-app` 只承载配送员高频执行动作
- `platform` 只承载监控、配置、审计与策略下发
- 低频管理能力默认收敛到“我的”或平台端，不挤占工作台主空间
