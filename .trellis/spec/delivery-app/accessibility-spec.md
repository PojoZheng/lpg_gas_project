# 无障碍规范

## 1. 基础要求
- 点击区域最小尺寸 `48x48dp`。
- 文字与背景对比度满足可读性要求。
- 关键按钮不可只依赖颜色区分。

## 2. 语义与读屏
- 所有关键控件需提供中文语义标签。
- 图标按钮必须有中文可读名称。
- 动态状态变化要提供可读文本描述。

## 3. 字体与缩放
- 支持系统字体缩放，不得出现关键内容截断。
- 核心金额与状态在放大场景仍保持可读。

## 4. 文案规范
- 全站中文文案统一，避免中英混杂。
- 严禁 emoji 作为信息传递主体。
